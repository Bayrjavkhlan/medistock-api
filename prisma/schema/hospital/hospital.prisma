model Hospital {
    id String @id @default(uuid())

    name        String
    address     String?
    phoneNumber String?
    email       String?

    address  Address?
    users User[]

    createdBy String?
    updatedBy String?
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}